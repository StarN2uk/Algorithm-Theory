#include<stdio.h>

int main()
{
	int x;
	char a[100];
	char b[100];
	scanf("%d", &x);
	scanf("%s %s", &a, &b);
	for(int i = 0; a[i]; i++)
	{
		for(int j = 0; b[j]; j++)
		{
			
		}
	}
	return 0;
}
