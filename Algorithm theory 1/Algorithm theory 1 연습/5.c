#include<stdio.h>

int main()
{
	int a, b, c, d;
	char c1, c2, c3;
	int sum;
	scanf("%d %d %d %d", &a, &b, &c, &d);
	sum = a + b + c + d;
	if (sum == 1)
	{
		printf("A\n");
	}
	else if (sum == 2)
	{
		printf("B\n");
	}
	else if (sum == 3)
	{
		printf("C\n");
	}
	else if (sum == 0)
	{
		printf("D\n");
	}
	else if (sum == 4)
	{
		printf("E\n");
	}
	scanf("%d %d %d %d", &a, &b, &c, &d);
	sum = a + b + c + d;
	if (sum == 1)
	{
		printf("A\n");
	}
	else if (sum == 2)
	{
		printf("B\n");
	}
	else if (sum == 3)
	{
		printf("C\n");
	}
	else if (sum == 0)
	{
		printf("D\n");
	}
	else if (sum == 4)
	{
		printf("E\n");
	}
	scanf("%d %d %d %d", &a, &b, &c, &d);
	sum = a + b + c + d;
	if (sum == 1)
	{
		printf("A\n");
	}
	else if (sum == 2)
	{
		printf("B\n");
	}
	else if (sum == 3)
	{
		printf("C\n");
	}
	else if (sum == 0)
	{
		printf("D\n");
	}
	else if (sum == 4)
	{
		printf("E\n");
	}
	scanf("%d %d %d %d", &a, &b, &c, &d);
	sum = a + b + c + d;
	if (sum == 1)
	{
		printf("A\n");
	}
	else if (sum == 2)
	{
		printf("B\n");
	}
	else if (sum == 3)
	{
		printf("C\n");
	}
	else if (sum == 0)
	{
		printf("D\n");
	}
	else if (sum == 4)
	{
		printf("E\n");
	}
	return 0;
}
